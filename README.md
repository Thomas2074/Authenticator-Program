# Authenticator-Program
This project is a simple authenticator program.

Made changes to generate a 6-digit number every 30 seconds, remove existing users, and list all current users. The users are stored in a JSON file.
